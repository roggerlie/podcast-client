import React from "react";
import logo from '../logo.svg';

const Header = () => (
    <header>
        <center>
            <img src={logo} alt="logo"/>
        </center>
    </header>
)

export default Header;